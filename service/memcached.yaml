apiVersion: v1
kind: Namespace
metadata:
  name: memcached
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: memcached
  namespace: memcached
spec:
  selector:
    matchLabels:
      app: memcached
  template:
    metadata:
      labels:
        app: memcached
    spec:
      containers:
      - image: memcached:1-alpine
        name: memcached
---
# TODO: 서비스 만들기
